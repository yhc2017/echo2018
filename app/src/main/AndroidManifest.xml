<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.echo.quick.activities">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.echo.quick.utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".Words2Activity" -->
        <!-- android:label="@string/title_activity_words2" -->
        <!-- android:theme="@style/AppTheme" /> -->
        <activity android:name=".WordsActivity" />
        <activity android:name=".UserMessageActivity" />
        <activity android:name=".ModifyPasswordActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".StrangeWordsListActivity" />
        <!-- <activity android:name=".ListShowActivity" /> -->
        <activity android:name=".ReadActivity" />
        <activity android:name=".ReadingAnswerActivity" />
        <activity android:name=".ReadingTranslateActivity" />
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".ReadingMianActivity" />
        <activity android:name=".InitPlanActivity"></activity>
    </application>

</manifest>